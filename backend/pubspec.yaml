name: queueless_backend
description: QueueLess backend server - a queue management system built in Dart.
version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  shelf_cors_headers: ^0.1.5
  crypto: ^3.0.6
  dart_jsonwebtoken: ^2.14.1

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
